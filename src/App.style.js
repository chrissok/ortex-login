import {style} from 'typestyle'

export const styles = {
  container: style({
    backgroundColor: '#141a23',
    height: '100vh',
    display: "flex",
    flexDirection: "column",
    justifyContent: 'center',
    alignItems: 'center'
  })
}